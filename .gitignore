node_modules/
dist/
.DS_Store
local/
logs/
coverage/


# Local env (never commit)
.env
.env.local

# Screenshot verification harness — captured PNGs (regenerate with scripts/screenshots/*)
screenshots/actual/
screenshots/mock/
screenshots/compare.html

.env.prod
.env.*

# ugly-studio state — throwaway local dev/IDE state (dev-server log, session &
# codebase index, sandbox, bundled minio/postgres). None of it is committed.
.ugly-studio/
